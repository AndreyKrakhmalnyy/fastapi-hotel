## Формирование окружения с установкой пакетов и зависимостей (с учетом установленного poetry - sudo/brew install poetry):
    - just init venv

## Установка хуков:
    - just install hooks

## Запуск форматтеров и линтеров перед отправкой:
    - just run checks
